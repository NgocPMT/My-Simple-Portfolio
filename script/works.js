const changeDescription = (text) => {
  document.querySelector(".description").innerHTML = text;
}

const resetDescription = () => {
  document.querySelector(".description").innerHTML = "Hover over these image!";
}